{
  "name": "pitchaware-babel",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel src/ --out-dir dist/ --source-maps", 
    "watch": "npm run compile --watch",
    "connect": "node dist/connect.js",
    "disconnect": "npm run watch && node dist/disconnect.js"
  },
  "keywords": [],
  "author": "RK Star",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.10.1",
    "@babel/core": "^7.10.2",
    "@babel/preset-env": "^7.10.2",
    "https": "^1.0.0",
    "net": "^1.0.2",
    "rxjs": "^6.5.5"
  }
}
